<section class="page-section about-heading">
    <div class="container">
        <img class="img-fluid rounded about-heading-img mb-3 mb-lg-0" src="{{ asset('img/' . $stores[26]->text) }}"
            alt="">
        <div class="about-heading-content">
            <div class="row">
                <div class="col-xl-9 col-lg-10 mx-auto">
                    <div class="bg-faded rounded p-5">
                        <h2 class="section-heading mb-4">
                            <span class="section-heading-upper">{{ $stores[20]->text }}</span>
                            <span class="section-heading-lower">{{ $stores[21]->text }}</span>
                        </h2>
                        <p>{{ $stores[22]->text }}</p>
                        <p class="mb-0">{{ $stores[23]->text }}
                            <em>{{ $stores[24]->text }}</em>
                            {{ $stores[25]->text }}
                        </p>
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>
